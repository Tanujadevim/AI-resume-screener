[phases.setup]
nixPkgs = ["poppler_utils", "tesseract"]

[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py migrate --run-syncdb", "python manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn core.wsgi --log-file -"